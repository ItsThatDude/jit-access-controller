---
sidebar_position: 2
description: Installing Jit-access manually with kubectl
---

# Install Manually

## Installation

You can manually install Jit-access by using the `install.yaml` file in the `dist` folder of the repository.

```sh
kubectl apply -f install.yaml
```
