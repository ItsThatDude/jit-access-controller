apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
    labels:
        app.kubernetes.io/managed-by: {{ .Release.Service }}
        app.kubernetes.io/name: {{ include "jit-access-controller.name" . }}
        helm.sh/chart: {{ .Chart.Name }}-{{ .Chart.Version | replace "+" "_" }}
        app.kubernetes.io/instance: {{ .Release.Name }}
    name: {{ include "jit-access-controller.resourceName" (dict "suffix" "accessresponse-approver-role" "context" $) }}
rules:
    - apiGroups:
        - access.antware.xyz
      resources:
        - accessresponses
      verbs:
        - create
        - get
        - list
        - watch
    - apiGroups:
        - access.antware.xyz
      resources:
        - accessresponses/status
      verbs:
        - get
