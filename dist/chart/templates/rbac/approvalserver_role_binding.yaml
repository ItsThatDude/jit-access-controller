{{- if and .Values.rbac.enable .Values.approvalServer.enable }}
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  labels:
    {{- include "chart.labels" . | nindent 4 }}
  name: approver-rolebinding
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: approver-role
subjects:
- kind: ServiceAccount
  name: {{ .Values.approvalServer.serviceAccountName }}
  namespace: {{ .Release.Namespace }}
{{- end -}}
