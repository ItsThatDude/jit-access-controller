---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: jitaccessrequests.access.antware.xyz
spec:
  group: access.antware.xyz
  names:
    kind: JITAccessRequest
    listKind: JITAccessRequestList
    plural: jitaccessrequests
    singular: jitaccessrequest
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: JITAccessRequest is the Schema for the jitaccessrequests API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of JITAccessRequest
            properties:
              durationSeconds:
                description: Duration in seconds (e.g. 600 for 10 min)
                format: int64
                type: integer
              role:
                description: Name of the Role
                type: string
              roleKind:
                description: Type of Role - Role or ClusterRole
                type: string
              subject:
                type: string
            required:
            - durationSeconds
            - role
            - roleKind
            - subject
            type: object
          status:
            description: status defines the observed state of JITAccessRequest
            properties:
              expiresAt:
                description: Timestamp when the access will expire
                format: date-time
                type: string
              requestId:
                description: ID of the access request
                type: string
              state:
                description: State of the Access Request
                type: string
            required:
            - requestId
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
