---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: clusterjitaccessrequests.access.antware.xyz
spec:
  group: access.antware.xyz
  names:
    kind: ClusterJITAccessRequest
    listKind: ClusterJITAccessRequestList
    plural: clusterjitaccessrequests
    singular: clusterjitaccessrequest
  scope: Cluster
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ClusterJITAccessRequest is the Schema for the clusterjitaccessrequests
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of ClusterJITAccessRequest
            properties:
              clusterRole:
                description: Name of the ClusterRole (must exist in same namespace)
                type: string
              durationSeconds:
                description: Duration in seconds (e.g. 600 for 10 min)
                format: int64
                type: integer
              subject:
                type: string
            required:
            - clusterRole
            - durationSeconds
            - subject
            type: object
          status:
            description: status defines the observed state of ClusterJITAccessRequest
            properties:
              expiresAt:
                description: Timestamp when the access will expire
                format: date-time
                type: string
              granted:
                description: True if access has been granted
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
