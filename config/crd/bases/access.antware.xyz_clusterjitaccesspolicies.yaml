---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: clusterjitaccesspolicies.access.antware.xyz
spec:
  group: access.antware.xyz
  names:
    kind: ClusterJITAccessPolicy
    listKind: ClusterJITAccessPolicyList
    plural: clusterjitaccesspolicies
    singular: clusterjitaccesspolicy
  scope: Cluster
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ClusterJITAccessPolicy is the Schema for the clusterjitaccesspolicies
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of ClusterJITAccessPolicy
            properties:
              policies:
                items:
                  description: ClusterSubjectPolicy defines access rules for a single
                    subject (user/serviceaccount).
                  properties:
                    allowedNamespaces:
                      description: AllowedNamespaces is a list of namespaces the subject
                        can request access to.
                      items:
                        type: string
                      type: array
                    allowedRoles:
                      description: AllowedRoles is a list of roles the subject is
                        allowed to request.
                      items:
                        type: string
                      type: array
                    maxDurationSeconds:
                      description: MaxDurationSeconds is the max duration for temporary
                        access.
                      format: int64
                      type: integer
                    subject:
                      description: Subject is the identity (email or K8s username)
                        of the user.
                      type: string
                  required:
                  - allowedNamespaces
                  - allowedRoles
                  - maxDurationSeconds
                  - subject
                  type: object
                type: array
            required:
            - policies
            type: object
          status:
            description: status defines the observed state of ClusterJITAccessPolicy
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
